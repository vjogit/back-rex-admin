version: "2"
sql:

  - engine: "postgresql"
    queries: "pkg/user/query.sql"
    schema: "../back-rex-common/schema.sql"
    gen:
      go:
        package: "user"
        out: "pkg/user"
        sql_package: "pgx/v5"
        omit_unused_structs: true
        emit_json_tags: true

  - engine: "postgresql"
    queries: "pkg/cohorte/query.sql"
    schema: "../back-rex-common/schema.sql"
    gen:
      go:
        package: "cohorte"
        out: "pkg/cohorte"
        sql_package: "pgx/v5"
        omit_unused_structs: true
        emit_json_tags: true

  - engine: "postgresql"
    queries: "pkg/feedback/query.sql"
    schema: "../back-rex-common/schema.sql"
    gen:
      go:
        package: "feedback"
        out: "pkg/feedback"
        sql_package: "pgx/v5"
        omit_unused_structs: true
        emit_json_tags: true


